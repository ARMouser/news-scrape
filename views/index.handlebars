<!DOCTYPE html>
<html lang='en'>

    <body>
        <header>
            <ul>
                <li id="home">
                    <a href='/'> Home </a>
                </li>
                <li id="scrape">
                    <a href='/scrape'> Get Scraping </a>
                </li>
                <li id="saved">
                    <a href='/saved'> Saved Links </a>
                </li>
            </ul>
        </header>
            <h1> News </h1>
        <div='news'>
            {{#if result}}
               {{#each result}}
               <h1> {{this.title}} </h1>
               <h2> {{this.link}} </h2>
               {{/each}}
            {{else}}
            No news retrieved. Hit "Get Scraping" to get started.
            {{/if}}
        </div>
    </body>
</html>